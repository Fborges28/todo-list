import {FormByReference} from "@/features";

export default function FormReference() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      <FormByReference />
    </main>
  )
}
